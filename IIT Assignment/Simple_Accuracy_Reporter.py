# Asking the user to enter a floating-point accuracy value
accuracy = float(input("Enter the model accuracy: "))

# Printing the value using an f-string with a descriptive message
print(f"Model accuracy is {accuracy}")